{
  "rules": {
    "users": {
      "$uid": {
        //".write": "$uid === auth.uid",
        //".read":"$uid === auth.uid"
        ".write": "auth.uid != null",
        ".read": "auth.uid != null"
      }
    }
  }
}